const Login = () => {

    const onLogin = (e) => {
        
    }


    return (
        <form onSubmit={onLogin}>
            <button type="submit">Login</button>
        </form>
    )
}

export default Login